const ComingSoon = ({ title, area }) => (
  <section className="empty-state">
    <span>{area}</span>
    <h2>{title}</h2>
    <p>This module is reserved for the next implementation phase.</p>
  </section>
);

export default ComingSoon;
